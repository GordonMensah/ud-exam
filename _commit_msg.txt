feat: guarantee one question per scripture reference

generate_question_pool:
- target = max(base_target, len(refs)): scales to match ref count
- New _try_any(ref): exhausts all question types deterministically
  before giving up, ensuring every ref that has any extractable
  content gets a question.
- First pass uses _try_any with no early break (all refs attempted).
- Second/third passes fill remaining slots with weighted-random types.

_extract_refs_with_context:
- Fix A: ellipsis-delimited quotes (…verse text…) now track their
  delimiter type; lowercase is allowed for ellipsis quotes since the
  author deliberately truncates verse text with "…". For ellipsis
  quotes with embedded scripture refs, text before the ref is used.
- Fix B: overlapping lookahead scan catches alternating …A…B…C…
  quotes that re.finditer skips because consecutive matches consume
  the shared delimiter.
- Fix C: for window sentences containing embedded scripture citations
  (e.g. "2 Sam 18:12 This person was loyal"), the text after the
  last citation is extracted as additional commentary/topics.
- Fix D: second pass for refs still bare after the midpoint-clamped
  first pass: widen the backward window to 600 chars using only the
  numbered-point block boundary (skip midpoint clamp) so that refs
  in densely-packed execution/subtitle lists can reach nearby content.

Result: all 164 scripture refs across 12 chapters now have at least
one question (241 questions total, up from 180).
