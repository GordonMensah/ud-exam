fix: prevent body-text from being captured as scripture quote

Two targeted fixes for Chapter 10 Proverbs 17:13 issue:

1. Point boundary detection: added lookbehind for digit+space in
   _NUMBERED_POINT_RE so labels like f) that follow a scripture
   reference end (e.g. Proverbs 17:13 f)) are detected.

2. Quote filter: reject quotes containing internal sentence breaks.
   Body text between two uses of quoted word muddy was being captured
   as a 180-char scripture quote. Real verse quotes are single clauses.

Regenerated all 180 questions.
