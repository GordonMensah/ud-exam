fix: clean reason-phrase extraction + gleaned-from prefix

_extract_patterned_lists:
- Replace fragile lookbehind _REASON_POINT_RE with simple \b\d+\. pattern
- Add _BODY_STARTER_RE + _trim_reason_phrase() to strip body commentary
  that follows reason titles with no punctuation delimiter
  ("To fight the fifth column Very early..." → "To fight the fifth column")
- Ordinal subs now use pre-compiled list (_ORD_SUBS) inside function
- Drop items ending with a bare digit (prose-pattern artefact like "...is important 1")

_q_gleaned_from:
- _INCOMPLETE_REASON_RE prefix now lowercases first letter of matched phrase
  ("Loyalty is important To fight..." → "Loyalty is important to fight...")
