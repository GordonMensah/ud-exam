fix: correct quote→ref pairing + Satan/Lucifer alias dedup

quote pairing (Step 3 in _extract_refs_with_context):
- Replace midpoint-window pairing with nearest-after-ref logic.
  In this book the cited verse always appears BEFORE its scripture
  reference, so the correct pairing is the first ref at or after
  the quote's position (up to 500 chars away).
  Previously the midpoint scheme assigned "And he [Joab] took three
  darts" (right before 2 Sam 18:14) to 2 Sam 18:12 because the quote
  fell in 18:12's midpoint territory.

_extract_names / _NAME_ALIAS_GROUPS:
- Add _NAME_ALIAS_GROUPS constant for names that refer to the same
  biblical entity (Lucifer = Satan = the dragon).
- When both names in a group appear in the chapter text, keep only
  the canonical (first-in-group) form so they can't both land as
  separate answer options in the same question.
