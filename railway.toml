[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run quiz_app/ui/streamlit_app.py --server.port $PORT --server.headless true --server.enableCORS false --server.enableXsrfProtection false"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
